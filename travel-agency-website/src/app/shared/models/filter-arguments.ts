export type FilterArguments = {
    minPrice: number
    maxPrice: number
    fromDate: string
    toDate: string
    country: string
    minStars: number
    maxStars: number
    noReviews: boolean
}