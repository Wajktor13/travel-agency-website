import { FilterExcursionsPipe } from './filter-excursions.pipe';

describe('FilterExcursionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExcursionsPipe();
    expect(pipe).toBeTruthy();
  });
});
